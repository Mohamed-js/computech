<h1 class="text-center mb-3">
اضافة عناصر لمنتج موجود
</h1>

<div class="container">

<%= form_with(model: @item) do |form| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
        <% @item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-3">
    <%= form.label 'المنتج' %>
    <%= select_tag "item[product_id]", options_from_collection_for_select(@products, "id", "name"), class:"form-control", prompt: "اختر المنتج", required: true %>
  </div>

  <div class="field mb-3">
    <%= form.label "سعر شراء الوحدة" %>
    <%= form.number_field :buying_price, class:"form-control", step: :any, required: true%>
  </div>

  <div class="field mb-3">
    <%= form.label "الكمية" %>
    <%= form.number_field :quantity, class:"form-control", required: true %>
  </div>

  <div class="actions my-3">
    <%= form.submit 'اضافة', class:"btn bg-gradient-tertiary white", data: { disable_with: "انتظر..." } %>
  </div>
<% end %>

<%= link_to 'الرجوع', root_path, class:"btn btn-secondary" %>
</div>